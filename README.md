# Context Vault App

Web UI for [Context Vault](https://context-vault.com). Built with React Router, React Query, Tailwind CSS 4, and shadcn/ui.

Supports **local mode** (SQLite vault, no auth — run `context-vault ui`) and **hosted mode** (API key auth against the hosted API).
